<h2>Редактировать тему</h2>
<div class="row">
    <div class="col-md-12 col-lg-12">
    <form action="/admin/edit/{{theme._id}}" method="POST">
        <div class="form-group">
            <label>Заголовок</label><br>
            <input type="text" name="title" class="form-control" value="{{theme.title}}"/>
        </div>
        <div class="form-group">
            <label>Описание</label><br>
            <textarea id="description" name="description" class="form-control" rows="5" >{{theme.description}}</textarea>
        </div>
        <input type="submit" value="Отправить" class="btn btn-primary"/>
    </form>
    </div>
</div>